# covid-19
To monitor the required states data and visualise it in command line

To configure which state and which district data you want to see, 
edit the config.json

Run `npm install`

Run `npm start`
